<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:id="@+id/includeDetailRl"
	android:background="@color/titlebar"
	android:layout_width="match_parent"
	android:layout_height="40dp" >

	<TextView
		android:id="@+id/includeDetailLblTitle"
		style="@style/TitleBarFontStyle"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:layout_centerHorizontal="true"
		android:layout_centerVertical="true"
		android:text="@string/include_title" />

	<ImageView
		android:id="@+id/includeDetailImgBack"
		style="@style/OperateIconStyle"
		android:layout_alignParentLeft="true"
		android:src="@drawable/back_default" />

	<ImageView
		android:id="@+id/includeDetailImgMore"
		style="@style/OperateIconStyle"
		android:layout_marginRight="5dp"
		android:layout_alignParentRight="true"
		android:src="@drawable/more_default" />

	<TextView
		android:layout_width="fill_parent"
		android:layout_alignParentBottom="true"
		android:background="@color/colorPrimary"
		android:layout_height="1dp" />

</RelativeLayout>